# IP Scout - base requirements (all environments)

# Core framework
Django>=5.2,<5.3
djangorestframework>=3.15
django-filter>=24.2
django-cors-headers>=4.3
drf-spectacular>=0.27

# Auth
djangorestframework-simplejwt>=5.3

# Database (PostgreSQL, network types: inet/cidr)
psycopg[binary]>=3.1

# Async / task queue
celery>=5.3
django-celery-beat>=2.6
django-celery-results>=2.5
redis>=5.0

# SSH connectivity to monitored servers
paramiko>=3.4

# Application-level encryption for stored SSH credentials
cryptography>=42.0

# GeoIP (MaxMind .mmdb reader) - optional at runtime; only imported when
# GEOIP_PROVIDER=maxmind is configured with a real database file
geoip2>=4.8

# Config / utilities
python-dotenv>=1.0
gunicorn>=22.0
